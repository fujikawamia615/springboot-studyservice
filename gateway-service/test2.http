### ========== Gateway 统一入口测试（端口 8082） ==========

### 1. 注册用户（通过 Gateway）
POST http://localhost:8082/api/user/register
Content-Type: application/json

{
    "username": "gateway_test",
    "password": "123456",
    "role": "ADMIN"
}

### 2. 登录（通过 Gateway，拿到 Token）
POST http://localhost:8082/api/user/login
Content-Type: application/json

{
    "username": "zhangsan",
    "password": "123456"
}

### 3. 通过 Gateway 访问 studysys 的 API（带 Token，应返回 200）
GET http://localhost:8082/api/student
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiI3Iiwicm9sZSI6InN0dWRlbnQiLCJpYXQiOjE3ODA1NzAzMjgsImV4cCI6MTc4MTE3NTEyOH0.je5nonqXcezo7pjr18aoLW0eDsO5p4yRtVDpw3OR_F0

### 4. 通过 Gateway 访问 studysys 的 API（不带 Token，应返回 401）
GET http://localhost:8082/api/student

### 5. 通过 Gateway 访问 studysys 的 API（带无效 Token，应返回 401）
GET http://localhost:8082/api/student
Authorization: Bearer invalid_token_123

### 6. 通过 Gateway 访问 user-service 的受保护接口（带 Token，应返回 200）
GET http://localhost:8082/api/user/7
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiI3Iiwicm9sZSI6InN0dWRlbnQiLCJpYXQiOjE3ODA1NTEyMzIsImV4cCI6MTc4MTE1NjAzMn0.dPG1vzccLrq56M6vhq8Sirk61gVh02BZHTxvrSb0yuc

### 7. 通过 Gateway 访问 user-service 的受保护接口（不带 Token，应返回 401）
GET http://localhost:8082/api/user/1

### 8. 直接访问 user-service（不走 Gateway，应正常）
POST http://localhost:8081/api/user/login
Content-Type: application/json

{
    "username": "zhangsan",
    "password": "123456"
}